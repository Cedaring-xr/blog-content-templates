---
title: 'Lambda Python package installs'
date: '2023-12-01'
tags: ['AWS Lambda', 'serverless', 'Python']
read-time: '~5 min'
---

Details on creating custom packages for use in Lambda Layers

```
export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void }) {
useEffect(() => {
// Log the error to an error reporting service
console.error(error)
}, [error])

    return (
    	<div>
    		<h2>Something went wrong!</h2>
    		<button
    			onClick={
    				// Attempt to recover by trying to re-render the segment
    				() => reset()
    			}
    		>
    			Try again
    		</button>
    		<p>
    			Or go back to <Link href="/">Home page</Link>
    		</p>
    	</div>
    )
}
```

<CustomImage
	src="https://raw.githubusercontent.com/cedaring-xr/blog-content-templates/main/images/blackandRedSunset.png"
	alt="placeholder for now"
	priority="true"
/>
